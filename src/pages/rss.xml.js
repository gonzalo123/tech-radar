import rss from '@astrojs/rss'; import {published,slug} from '../lib/news'; export async function GET(context){const items=await published(); return rss({title:'gonzalo123 Tech Radar',description:'Un radar personal sobre tecnología, software y sistemas.',site:context.site,items:items.map(x=>({title:x.data.title,description:x.data.description,pubDate:x.data.date,link:`/news/${slug(x)}/`}))});}
